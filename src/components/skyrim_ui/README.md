# Skyrim UI

This folder includes self-contained UI elements that are in the style of [Skyrim's UI](https://en.wikipedia.org/wiki/The_Elder_Scrolls_V:_Skyrim). These components have no dependencies (outside of React) and use inline SVG and CSS, so it's all pure TSX.
